<h1><%= t('views.common.title.edit', name: Book.model_name.human) %></h1>

<%= render 'form', book: @book %>

<%= link_to t('views.common.edit'), @book %> |
<%= link_to t('views.common.back'), books_path %>
